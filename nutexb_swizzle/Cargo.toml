[package]
name = "nutexb_swizzle"
version = "0.1.0"
authors = ["ScanMountGoat <>"]
edition = "2018"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
arbitrary = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
rand = "0.8.4"